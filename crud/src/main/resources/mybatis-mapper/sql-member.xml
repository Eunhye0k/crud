<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.example.crud.Mapper.MemberMapper">

	<insert id="addUserId" parameterType="Member">
	INSERT INTO member(m_id, m_name, m_password, m_address) 
	VALUES (#{mId},#{mName},#{mPassword},#{mAddress})
	</insert>
	
	<select id="login" parameterType="hashMap"
		resultType="com.example.demo.model.Member">
		SELECT id 
		FROM member 
		WHERE m_id = #{mId} AND m_passowrd = #{mPassword}
	</select>

</mapper>